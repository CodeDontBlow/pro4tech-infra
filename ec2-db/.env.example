# ─── PostgreSQL ──────────────────────────────────────────────
POSTGRES_USER=pro4tech
POSTGRES_PASSWORD=           # ← mesma senha do .env da ec2-back
POSTGRES_DB=pro4tech

# ─── MinIO ───────────────────────────────────────────────────
MINIO_ACCESS_KEY=            # ← mesma chave do .env da ec2-back
MINIO_SECRET_KEY=            # ← mesma senha do .env da ec2-back (mínimo 8 chars)
MINIO_BUCKET=pro4tech